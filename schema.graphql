### This file was generated by Nexus Schema
### Do not make changes to this file directly


type Authentication {
  token: String!
  user: User!
}

type ContentBlock {
  data: JSONObject!
  id: ID!
  interaction: Interaction!
  type: ContentBlockType!
}

enum ContentBlockType {
  IMAGE
  LIST
  TEXT
}

type Device {
  brand: String!
  id: ID!
  model: String!
  overlays: [Overlay!]!
  user: User!
}

type Interaction {
  blocks: [ContentBlock!]!
  height: Float!
  id: ID!
  overlay: Overlay!
  text: String!
  width: Float!
  x: Float!
  y: Float!
}

"""
The `JSONObject` scalar type represents JSON objects as specified by [ECMA-404](http://www.ecma-international.org/publications/files/ECMA-ST/ECMA-404.pdf).
"""
scalar JSONObject

type Mutation {
  createDevice(brand: String!, model: String!): Device!
  createOverlay(deviceId: ID!, name: String!): Overlay!
  createUser(email: String!, password: String!): User!
  deleteUser(id: ID!): User!
  login(email: String!, password: String!): Authentication!
}

type Overlay {
  device: Device!
  id: ID!
  interactions: [Interaction!]!
  name: String!
}

type Query {
  device(id: ID!): Device
  devices: [Device!]!
  overlays: [Overlay!]!
  user(id: ID!): User
  users: [User!]!
}

type User {
  devices: [Device!]!
  email: String!
  id: ID!
  role: String!
}
